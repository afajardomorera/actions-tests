name: '[PR main] Call required checks'

on:
  pull_request:
    branches:
      - main
  workflow_call:

jobs:
  main-pull-request:
    uses: ./.github/workflows/delay_on_pr.yml
    with:
      time: 5m
