name: Autoapprove PR

on:
  issue_comment:
    types: [created]

jobs:
  process-deployment-command:
    runs-on: ubuntu-20.04
    timeout-minutes: 5
    outputs:
      rama: ${{ steps.get_timestamp.outputs.rama }}
    steps:
      - id: checkout
        name: Check Out y cambio a rama dev
        uses: actions/checkout@v3
        with:
          ref: main
      
      - name: get_timestamp
        id: get_timestamp
        run: |
          echo $(date +'%Y%m%d%H%M%S')
          echo "rama=deploy$(date +'%Y%m%d%H%M%S')" >> $GITHUB_OUTPUT

      - id: create_branch_and_commit_it
        name: "[EKS] Create Brancha and commit EKS Image"
        run: |
          sudo apt-get install sed
          git config user.name "action-tests[bot]"
          git config user.email "action-tests[bot]@users.noreply.github.com"
          git checkout -b ${{ steps.get_timestamp.outputs.rama}} main
          sed -i 's/IMAGEN/IMAGEN2:prueba666/' ./terraform.tfvars
          git status
          git add -A
          git commit -m "Updated file for deploying image: DONE!!"
          git push --set-upstream origin ${{ steps.get_timestamp.outputs.rama}}

      - name: Crear PR
        id: pr_creation
        uses: peter-evans/create-pull-request@v3
        with:
          branch: ${{ steps.get_timestamp.outputs.rama }}
          base: main
          title: "[DEPLOY BY COMMENT] Automatic PR ${{ steps.get_timestamp.outputs.rama }}"
          body:
            Prueba de PR automática - ${{ steps.get_timestamp.outputs.rama }}
          token: ${{ secrets.GITHUB_TOKEN }}
          delete-branch: false
      
      #- name: Aprobar Pull Request
      #  uses: juliangruber/approve-pull-request-action@v1
      #  with:
      #    github-token: ${{ secrets.GITHUB_TOKEN }}
      #    number: ${{ steps.pr_creation.outputs.pull-request-number }}
      
      #- name: Merge Pull Request
      #  uses: juliangruber/merge-pull-request-action@v1
      #  with:
      #    github-token: ${{ secrets.GITHUB_TOKEN }}
      #    number: ${{ steps.pr_creation.outputs.pull-request-number }}
      #    method: squash
      
          
